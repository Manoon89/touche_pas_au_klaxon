{% extends 'base.html.twig' %}

{% block title %}Modifier une agence{% endblock %}

{% block body %}
    <h1>Modifier une agence</h1>

    {{ include('agency/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="return" href="{{ path('agency_index') }}">Retour à la liste des agences</a>
    <a class="return" href="{{ path('home') }}">Retour à la page d'accueil</a>

{% endblock %}
